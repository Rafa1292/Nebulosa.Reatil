@page "/Products/Update/{ProductId}"

<UpdateProductComponent ProductId="@ProductId" />

@code {
    [Parameter]
    public string ProductId { get; set; }

    protected override async Task OnInitializedAsync()
    {
        ProductId = ProductId;
    }
}
