@page "/Brands/Update/{BrandId}"

<UpdateBrandComponent BrandId="@BrandId" />

@code {
    [Parameter]
    public string BrandId { get; set; }

    protected override async Task OnInitializedAsync()
    {
        BrandId = BrandId;
    }
}